# profiles.yml
dbt_workflow:
  target: dev
  outputs:
    dev:
      type: postgres
      threads: 1
      host: localhost
      port: 5432
      user: postgres
      pass: dbt_app_password
      dbname: postgres
      schema: staging

